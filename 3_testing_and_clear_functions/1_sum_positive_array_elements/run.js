const readline = require('readline');
const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});

console.log(`
run.js

You open calc Sum of positive array elements by using clear functions and testing on Node.js!
Версия: 1.0.0.

Using: user must should input an array with numbers.
`);
